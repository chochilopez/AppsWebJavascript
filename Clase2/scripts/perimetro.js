// pi en constante no reasignar 
const NUMERO_PI = 3.14;
let radio = 10;
let resultado = 2 * NUMERO_PI * radio;
console.log('Tu circulo, con radio de ' + radio + ' tiene un perimetro de:', resultado);
